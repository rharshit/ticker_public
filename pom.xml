<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.ticker</groupId>
    <artifactId>core</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Core</name>


    <modules>
        <module>home</module>
        <module>fetcher</module>
        <module>mock-fetcher</module>
        <module>brokerage</module>
        <module>booker</module>
        <module>common</module>
        <module>template</module>
        <module>bb-rsi</module>
        <module>bb-rsi-safe</module>
        <module>mwave</module>
        <module>notification</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <ticker.home.version>0.0.1-SNAPSHOT</ticker.home.version>
        <ticker.fetcher.version>0.0.1-SNAPSHOT</ticker.fetcher.version>
        <ticker.mock-fetcher.version>0.0.1-SNAPSHOT</ticker.mock-fetcher.version>
        <ticker.brokerage.version>0.0.1-SNAPSHOT</ticker.brokerage.version>
        <ticker.booker.version>0.0.1-SNAPSHOT</ticker.booker.version>
        <ticker.common.version>0.0.1-SNAPSHOT</ticker.common.version>
        <ticker.template.version>0.0.1-SNAPSHOT</ticker.template.version>
        <ticker.bb-rsi.version>0.0.1-SNAPSHOT</ticker.bb-rsi.version>
        <ticker.bb-rsi-safe.version>0.0.1-SNAPSHOT</ticker.bb-rsi-safe.version>
        <ticker.mwave.version>0.0.1-SNAPSHOT</ticker.mwave.version>
        <ticker.notification.version>0.0.1-SNAPSHOT</ticker.notification.version>

        <spring.boot.version>2.5.15</spring.boot.version>
        <spring.version>5.3.39</spring.version>
        <lombok.version>1.18.30</lombok.version>
    </properties>

    <dependencies>
        <!-- https://mvnrepository.com/artifact/org.springframework/spring-context -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
            <version>${spring.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <version>${spring.boot.version}</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
            <version>${lombok.version}</version>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring.boot.version}</version>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>